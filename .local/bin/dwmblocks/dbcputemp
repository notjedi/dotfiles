#!/usr/bin/env bash

temp=$(sensors | grep 'Package id' | cut -d ' ' -f5 | grep -o '[0-9]*[.].*')

case $BUTTON in
1) notify-send -u low "GPU temp:" "\n🔥 $(gputemp)" ;;
3) setsid -f "$TERMINAL" -e watch sensors ;;
esac

echo "🔥 $temp"
