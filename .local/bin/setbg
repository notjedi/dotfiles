#!/usr/bin/env bash

# if [ $# -eq 0 ]
if [ ! -z "$1" ]; then
    xwallpaper --zoom "$file"
    wal -n -i "$file";
    exit 0
fi

dirname="/mnt/Seagate/wallpapers"
file=$(find "$dirname" -type f | shuf -n 1)

# set bg
xwallpaper --zoom "$file"
wal -n -i "$file";
